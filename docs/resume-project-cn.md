# 中文简历项目经历

## 推荐版本

**DocMind-RAG：意图驱动的企业知识检索与可信问答平台**
`Python / FastAPI / Qwen2.5 / LoRA / PEFT / BM25 / RRF / Parent-Child Chunking / Docker`

- 将原文档工具与法律 RAG 项目重构为领域无关的企业知识分析平台，打通
  PDF、DOCX、XLSX、TXT、Markdown 上传解析、结构化分块、索引、检索与问答链路；
  保留页码、章节、表格及父子块关系，支持证据定位与引用。
- 设计可解释 Query Router，根据精确查询、语义问答、表格和跨版本比较动态组合
  BM25、Lite Dense、加权 RRF 与重排通道，并在响应中返回通道分数、排名、
  路由原因和阶段耗时。
- 设计通用意图识别与知识路由模块，提供 CPU Lite 降级基线，并实现基于 MASSIVE
  中文数据的 Qwen2.5-1.5B LoRA 训练、推理和评测流水线；将领域、意图、置信度
  接入知识库建议和检索权重，低置信度时回退全库检索，避免错误路由放大。
- 构建证据驱动的可信决策机制，通过相关性、多路一致性、排名间隔、引用覆盖率和
  冲突惩罚计算置信度；对版本、数值和否定冲突返回 `clarify`，证据不足时
  `abstain`，避免无依据生成。
- 建立可复现消融评测，保存运行环境、配置、原始预测及 MRR、Recall@K、nDCG、
  引用、拒答和冲突指标；在 7 篇文档、14 个问题的小型回归集上如实记录 BM25
  优于当前哈希 Dense 基线，未将负结果包装为模型提升。
- 将旧法律能力降级为延迟加载领域插件，移除硬编码路径与强制 CUDA；补充离线演示
  工作台、Request ID、结构化日志、非 root Docker 部署、GitHub Actions 和并发
  HTTP 基线脚本。

## 一页简历压缩版

**DocMind-RAG 意图驱动的企业知识检索与可信问答平台**

- 基于 FastAPI 构建 PDF/DOCX/XLSX 等异构文档接入、Parent-Child 分块及
  BM25/Dense/RRF 自适应检索链路，保留页码、章节、表格与可追溯证据引用。
- 设计证据相关性、跨文档冲突检测及五维置信度机制，实现
  `answer / clarify / abstain` 选择性回答，并暴露检索通道、排名和耗时。
- 基于 MASSIVE 构建 Qwen2.5 LoRA 意图分类与知识路由流水线，支持低置信度回退；
  建立分类、检索和可信问答评测，完成离线演示台、Docker 与 CI。

## 不能写入简历的表述

- “混合检索显著优于 BM25”：当前实测结论相反。
- “法律分类准确率达到某数值”：现有检查点没有可验证的最终测试指标。
- “意图 LoRA 达到某个 Accuracy/Macro-F1”或“路由使 Recall 提升某百分比”：
  训练评测脚本已完成，但全量 GPU 实验尚未产生可验证产物。
- “支持千万级文档”或“高并发生产部署”：尚未进行相应规模验证。
- “使用 Qwen3 Embedding/Cross-Encoder 获得提升”：接口已规划，真实实验待做。

## 核对说明

本文件基于当前项目代码、测试和实验产物生成。本任务环境未找到
`/mnt/data/朱振湘简历.pdf` 或同名本地副本，因此无法逐字对照原简历版式和旧项目
措辞；替换前应确认项目日期、个人职责和技术栈篇幅与整份简历一致。
