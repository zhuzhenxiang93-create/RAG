# Data v3 interview narrative

这次升级重在语义而非盲目扩量：独立 source adapter 隔离异构 schema；统一契约显式表达
缺失；隐私与判决泄漏是阻断门禁；先按同案/重复组聚合再时间划分；task mask 允许罪名、
刑罚、罚金、检索和鲁棒性任务使用不同有效子集。许可不明的数据只审计或拒绝，不静默混合。

诚实产物是 candidate。它主要改善可复现性和标签含义；200 条脱敏样本已完成人工复核并
全部通过。晋级正式 v3 仍需明确许可且带日期的官方源与全量近重复扫描。
